{
  "name": "purchaseorders",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "express": "^4",
    "@sap/cds-hana": "^2"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^7"
  },
  "scripts": {
    "start": "cds-serve"
  },
  "cds": {
    "requires": {
      "db": "hana",
      "gwsample": {
        "kind": "odata-v2",
        "model": "srv/external/gwsample",
        "csrf": true,
        "csrfInBatch": true,
        "[backend]": {
          "credentials": {
            "destination": "ES5"
          }
        },
        "[production]": {
          "credentials": {
              "destination": "ES5"
          }
        }
      }
    }
  }
}